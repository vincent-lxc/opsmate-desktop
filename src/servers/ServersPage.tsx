export function ServersPage() {
  return (
    <section>
      <h1>我的服务器</h1>
      <p>My Servers placeholder — cloud adapters land in later tasks.</p>
      <p>
        Terminal and AI workspace will open from a server detail route (
        <code>/servers/:serverId</code>), not from a top-level menu.
      </p>
    </section>
  );
}
