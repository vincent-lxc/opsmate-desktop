{
  "$schema": "https://schema.tauri.app/config/2/capability",
  "identifier": "default",
  "description": "Foundation WebView capability: core UI only. No shell, opener, deep-link, or stronghold plugin commands are exposed to the WebView.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
