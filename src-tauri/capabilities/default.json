{
  "$schema": "https://schema.tauri.app/config/2/capability",
  "identifier": "default",
  "description": "WebView capability: core UI only. Opener and deep-link plugins are registered in Rust for system-browser login and opsmate:// callback delivery; they must never be granted as WebView permissions. No shell or stronghold plugin commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
