{
  "$schema": "https://schema.tauri.app/config/2/capability",
  "identifier": "default",
  "description": "Main WebView only: core UI + named application IPC. No opener/deep-link/stronghold plugin commands are exposed to the WebView; those stay Rust-side.",
  "windows": ["main"],
  "local": true,
  "permissions": [
    "core:default",
    "core:event:default",
    "allow-desktop-ipc"
  ]
}
